# Databases and SQL — Jupyter Book

This repository contains a Jupyter Book version of the "Databases and SQL" workbook.
Build locally with:

```bash
pip install jupyter-book ghp-import
jupyter-book build .
ghp-import -n -p -f _build/html
```
